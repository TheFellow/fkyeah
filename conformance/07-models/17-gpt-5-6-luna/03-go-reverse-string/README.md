# 17-gpt-5-6-luna / 03-go-reverse-string

E2E smoke test: `gpt-5.6-luna` generates a Go go-reverse-string program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
