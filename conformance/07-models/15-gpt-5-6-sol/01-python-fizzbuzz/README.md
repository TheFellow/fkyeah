# 15-gpt-5-6-sol / 01-python-fizzbuzz

E2E smoke test: `gpt-5.6-sol` generates a Python python-fizzbuzz program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
