# 10-gpt-5-4 / 02-javascript-fibonacci

E2E smoke test: `gpt-5.4` generates a Javascript javascript-fibonacci program, a tool node extracts and runs it, and the test validates the expected output. Skips if `OPENAI_API_KEY` is not set.
