# 14-claude-sonnet-5 / 04-ruby-factorial

E2E smoke test: `claude-sonnet-5` generates a Ruby ruby-factorial program, a tool node extracts and runs it, and the test validates the expected output. Skips if `ANTHROPIC_API_KEY` is not set.
